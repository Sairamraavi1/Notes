I understand the stats export/import option now. Since gathering the schema statistics currently takes around eight hours, importing previously exported stats should be significantly faster because Oracle isn't rescanning the table data. My concern is around how we validate that those previous stats are still representative. Since we're using REPLACE, any structural changes would recreate the table from the latest source metadata, and the previous stats wouldn't contain statistics for newly added or changed columns. Even without structural changes, significant changes in row counts or data distribution could make the old stats inaccurate. So could we test the export/import approach and define a validation to identify tables that require fresh stats?”
