col DB_USER for a25
col os_user for a20
col OS_HOST for a55
col EXTENDED_TIMESTAMP for a35
set linesize 300 pages 300
SELECT *
FROM (
  SELECT DB_USER, OS_USER, OS_HOST, EXTENDED_TIMESTAMP
  FROM gv$XML_AUDIT_TRAIL
  WHERE RETURNCODE = '1017'
    AND DB_USER = 'GGADMIN'
  ORDER BY EXTENDED_TIMESTAMP DESC
)
WHERE rownum < 10;


SELECT DBUSERNAME, OS_USERNAME, USERHOST, EVENT_TIMESTAMP, RETURN_CODE
FROM unified_audit_trail
WHERE dbusername='CDE_QLAB02'
AND return_code=1017
ORDER BY event_timestamp ASC;