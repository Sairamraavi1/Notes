Doing object-by-object DBA staging would require pausing ADG and converting it to snapshot mode each time we create staging tables, which isn’t practical. To avoid repeated ADG pauses, a one-time filtered export of the required transactional data is the most feasible approach
